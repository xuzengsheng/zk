server:
  port: 7001

spring:
  application:
    name: nacos-feign
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

feign:
  #默认是不支持的，所以这里要开启，设置为true
  hystrix:
    enabled: true