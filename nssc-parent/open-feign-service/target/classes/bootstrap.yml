server:
  port: 7001

spring:
  application:
    name: nacos-feign  #服务名称
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848 #注册中心 nacos地址

feign:
  #设置熔断降级 默认为false
  hystrix:
    enabled: true